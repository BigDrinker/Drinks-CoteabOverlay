# Coteab Macro Overlay v2.2.1

- Session duration starts at `00:00:00` every time the application launches.
- The local Notice tab prepends the v2.2.1 bug-fix changelog and still displays it when the remote notice cannot be reached.
- Includes the earlier overlay shutdown, accurate runtime, and duplicate biome webhook fixes.
